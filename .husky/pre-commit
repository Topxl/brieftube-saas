echo "🔍 Running pre-commit checks..."

# Run TypeScript type check
echo "📝 TypeScript check..."
pnpm ts || exit 1

# Run ESLint
echo "🔨 ESLint check..."
pnpm lint || exit 1

echo "✅ All checks passed!"
