#!/bin/sh
echo "🔍 Running pre-commit checks..."

echo "📝 TypeScript checking..."
pnpm ts || exit 1

echo "🎨 Linting staged files..."
git diff --cached --name-only --diff-filter=ACMR | grep -E '\.(ts|tsx|js|jsx)$' | xargs -r pnpm eslint --max-warnings 0 --no-warn-ignored || exit 1

echo "✅ All checks passed! Proceeding with commit..."
